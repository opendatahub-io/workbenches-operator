# Prometheus Monitor Service (Metrics)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app.kubernetes.io/component: metrics
  name: workspaces-controller-metrics-monitor
spec:
  endpoints:
  - path: /metrics
    port: https
    scheme: https
    bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    tlsConfig:
      ca:
        configMap:
          name: openshift-service-ca.crt
          key: service-ca.crt
      serverName: workspaces-controller-metrics-service.NAMESPACE.svc
  # Do not select on part-of. workbenches-operator rewrites metadata part-of
  # to "workbenches" and does not update this selector. component+name uniquely
  # identify workspaces-controller-metrics-service in both overlays.
  selector:
    matchLabels:
      app.kubernetes.io/component: metrics
      app.kubernetes.io/name: workspaces-controller
